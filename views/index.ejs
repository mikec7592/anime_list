<!DOCTYPE html>
<html lang="en">
<head>
   <%- include ('partials/head.ejs') %>
</head>
<body>
    <h1>Anime Index</h1>
    <nav> <button><a href="/animeList/new">New Entry</a></button> </nav>
            <div class="container">
                <% for(let i = 0; i < anime.length; i++) { %> 
                <div class="index">

                    <a href="/animeList/<%=anime[i].id%>"><%=anime[i].title%></a> 
                    <% if (anime[i].completed === true) { %>
                        Rated: <%= anime[i].rating %>
                   <% } else { %>
                        Plan to watch / Watching
                 <%  } %>
                 <img src="<%=anime[i].img%>" class="index">
                    <form action="/animeList/<%=anime[i].id%>?_method=DELETE" method="POST">
                        <input type="submit" value="REMOVE"/>
                    </form>
                    <button><a href="/animeList/<%=anime[i].id%>/edit">MODIFY</a></button>

                </div>
                <% } %>
              </div>
</body>
</html>